<?xml version="1.0" encoding="utf-8"?>
<!-- HTTPS everywhere, with cleartext allowed ONLY for the local dev backend
     (Android-emulator loopback, localhost, and the dev machine's LAN IP).
     Production traffic to the Railway HTTPS backend is unaffected. -->
<network-security-config>
    <base-config cleartextTrafficPermitted="false"/>
    <domain-config cleartextTrafficPermitted="true">
        <!-- Android-emulator loopback → the Mac hosting uvicorn -->
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <!-- Physical phone on the same WiFi as the dev Mac.
             Rebuild the APK whenever this IP changes. -->
        <domain includeSubdomains="false">192.168.1.42</domain>
    </domain-config>
</network-security-config>
