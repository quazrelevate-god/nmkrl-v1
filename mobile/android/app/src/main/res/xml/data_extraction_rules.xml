<?xml version="1.0" encoding="utf-8"?>
<!--
  Android 12+ (API 31) backup + device-transfer rules. Backup is already off
  via android:allowBackup="false"; these make the intent explicit and cover the
  device-transfer path too, so the session token, the PIN hash and any cached
  grievance data never leave the phone through a cloud backup or a phone-to-phone
  transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="." />
        <exclude domain="file" path="." />
        <exclude domain="database" path="." />
    </device-transfer>
</data-extraction-rules>
